{% comment %}
  Renders a header drawer menu for mobile and desktop.

  Usage:
  {% render 'header-drawer' %}
{% endcomment %}

<header-drawer data-breakpoint="{% if section.settings.menu_type_desktop == 'drawer' %}desktop{% else %}tablet{% endif %}">
  <details id="Details-menu-drawer-container" class="menu-drawer-container">
    <summary
      class="header__icon header__icon--menu header__icon--summary link focus-inset"
      aria-label="{{ 'sections.header.menu' | t }}"
    >
      <span>
        {{- 'icon-hamburger.svg' | inline_asset_content -}}
        {{- 'icon-close.svg' | inline_asset_content -}}
      </span>
    </summary>
    <div id="menu-drawer" class="gradient menu-drawer motion-reduce color-{{ section.settings.menu_color_scheme }}">
      <div class="menu-drawer__inner-container">
        <div class="menu-drawer__navigation-container">
          {%- render 'header-search-v2' -%}

          {%- for block in section.blocks -%}
            {%- case block.type -%}
              {%- when 'banner' -%}
                <div class="menu-drawer__banner" style="background-color: {{ block.settings.background_color }}; color: {{ block.settings.text_color }};">
                  <style>
                    .menu-drawer__banner {
                      font-size: {{ block.settings.font_size_mobile }}px;
                    }
                    @media screen and (min-width: 750px) {
                      .menu-drawer__banner {
                        font-size: {{ block.settings.font_size_desktop }}px;
                      }
                    }
                  </style>
                  {{ block.settings.banner_text }}
                </div>
              {%- when 'collection_block' -%}
                {%- if forloop.first -%}
                  <div class="menu-drawer__collection-blocks">
                {%- endif -%}
                <a href="{{ block.settings.link | default: "#" }}" class="menu-drawer__collection-block" style="background-color: {{ block.settings.background_color }}; color: {{ block.settings.text_color }};">

                <style>
                    .menu-drawer__collection-block .heading {
                      background-color: {{ block.settings.background_color }};
                      color: {{ block.settings.text_color }};
                  }
                  .menu-drawer__collection-block {
                      color: {{ block.settings.text_color }};
                      background-color: {{ block.settings.background_color }};
                  }
                </style>
                  {% if block.settings.image != blank %}
                    {{ block.settings.image | image_url: width: 400 | image_tag: loading: 'lazy' }}
                  {% endif %}
                  {% if block.settings.heading != blank %}
                    <span class="heading">{{ block.settings.heading }}</span>
                  {% endif %}
                </a>
                {%- if forloop.last -%}
                  </div>
                {%- endif -%}
            {%- endcase -%}
          {%- endfor -%}

          <nav class="menu-drawer__navigation">
            <ul class="menu-drawer__menu list-menu" role="list">
              {%- for link in section.settings.menu.links -%}
                <li class="menu-drawer__menu-item">
                  <a
                    href="{{ link.url }}"
                    class="menu-drawer__menu-item link link--text list-menu__item list-menu__item--link{% if link.active %} menu-drawer__menu-item--active{% endif %}"
                    {% if link.current %}
                      aria-current="page"
                    {% endif %}
                  >
                    {{ link.title | escape }}
                  </a>
                </li>
              {%- endfor -%}
            </ul>
          </nav>
        </div>
      </div>
    </div>
  </details>
</header-drawer>
